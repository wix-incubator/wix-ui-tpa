import * as React from 'react';
import { Dialog } from '../';
import { classes } from './DialogWiringExample.st.css';

export const DialogWiringExample = () => {
  return <Dialog className={classes.component} />;
};
