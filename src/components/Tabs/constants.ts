export enum SKIN {
  underline = 'underline',
  clear = 'clear',
}

export enum CONTENT_ALIGNMENT {
  center = 'center',
  left = 'left',
  right = 'right',
}

export enum CONTENT_WIDTH {
  stretch = 'stretch',
  shrink = 'shrink',
}
