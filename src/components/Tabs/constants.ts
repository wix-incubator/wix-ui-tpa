export enum SKIN {
  underline = 'underline',
  clear = 'clear',
}

export enum ALIGNMENT {
  center = 'center',
  left = 'left',
  right = 'right',
}

export enum VARIANT {
  fullWidth = 'fullWidth',
  standard = 'standard',
}
