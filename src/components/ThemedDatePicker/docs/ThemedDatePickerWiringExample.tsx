import * as React from 'react';
import { ThemedDatePicker } from '../';
import { classes } from './ThemedDatePickerWiringExample.st.css';

export const ThemedDatePickerWiringExample = () => {
  return <ThemedDatePicker className={classes.root} />;
};
